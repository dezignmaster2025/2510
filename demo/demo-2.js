const addNumbers = (num1, num2) => {
    return num1 + num2;
}

const subtractNumbers = (num1, num2)=>{
    return num1- num2;
}

const multiplyNumbers = (num1, num2)=>{
   return num1 * num2;
}

function divideNumbers(num1, num2) {
    return num1 / num2;
}

addNumbers(5, 10): //15
subtractNumbers(10, 2): //8
multiplyNumbers(20, 5); //100
divideNumbers(100, 5) //50